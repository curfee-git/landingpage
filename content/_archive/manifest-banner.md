# Manifest-Banner (entfernt am 2026-08-08, am selben Tag abends wieder eingebaut)

Wieder auf der Seite: zwischen „Wie ich entscheide" und „Was ich mache und was nicht",
Key heißt jetzt `manifest.text` statt `method.closer`. Der Alias-Anker in der About-Section
wurde dabei entfernt, `#manifest` zeigt wieder auf das echte Band.

## Ursprüngliche Entfernung (Vormittag)

Vollbreites Navy-Banner zwischen „Methode" und „Über mich", gerendert von
`src/components/ManifestSection.astro` (Komponente mit entfernt, siehe Git-Historie).

## `method.closer`

- de: Entscheidungen, die sich selbst durchsetzen
- en: Decisions that enforce themselves

Der Satz lebt weiter in den `schema.mechanism.*`-Keys (JSON-LD) und in llms.txt.
