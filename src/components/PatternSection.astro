---
import type { Translations } from '@/i18n/translations';

interface Props {
  translations: Translations;
}
const { translations: t } = Astro.props;

const cards = [
  { title: t['pattern.3.title'], text: t['pattern.3.reality'] },
  { title: t['pattern.1.title'], text: t['pattern.1.reality'] },
  { title: t['pattern.5.title'], text: t['pattern.5.reality'] },
  { title: t['pattern.7.title'], text: t['pattern.7.reality'] },
];
---

<section
  id="pattern"
  class="section-pad bg-surface-soft"
  aria-labelledby="pattern-heading"
>
  {/* Legacy anchor: external links to /#observations keep landing here */}
  <div id="observations" aria-hidden="true"></div>
  <div class="max-w-300 mx-auto px-gutter xl:px-0">
    <header class="max-w-184 mb-16">
      <p class="text-eyebrow text-brand-primary m-0">
        {t['pattern.subtitle.status_quo']}
      </p>
      <h2
        id="pattern-heading"
        data-speakable
        class="font-sans text-3xl md:text-4xl font-bold leading-[1.05] tracking-tight text-balance break-words hyphens-auto m-0 mt-6"
      >
        {t['pattern.heading.status_quo']}
      </h2>
    </header>

    <ul
      class="grid grid-cols-1 sm:grid-cols-2 gap-x-14 gap-y-10 m-0 p-0 list-none"
      role="list"
    >
      {cards.map((card) => (
        <li class="border-t border-ink-primary/13 pt-6 max-w-[62ch]">
          <h3 class="font-sans text-xl font-bold text-ink-primary m-0 mb-2">
            {card.title}
          </h3>
          <p class="text-body-medium text-ink-muted leading-relaxed m-0">{card.text}</p>
        </li>
      ))}
    </ul>
  </div>
</section>
