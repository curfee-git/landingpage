---
import { getRoutes } from '@/configurations/routes';
import type { Translations } from '@/i18n/translations';

interface Props {
  translations: Translations;
}
const { translations: t } = Astro.props;
const routes = getRoutes();
---

<section
  id="contact"
  class="py-20 sm:py-[104px] xl:py-[144px] bg-brand-primary text-brand-onPrimary"
  aria-labelledby="contact-heading"
>
  <div class="max-w-[1200px] mx-auto px-[var(--gutter)] xl:px-0 text-center max-w-[720px]">
    <h2
      id="contact-heading"
      class="font-sans text-[clamp(1.75rem,3vw,2.5rem)] font-extrabold leading-[1.1] tracking-[-0.015em] text-balance break-words hyphens-auto m-0 text-brand-onPrimary"
    >
      {t['calltoaction.heading']}
    </h2>
    <p class="font-sans text-[1.125rem] leading-[1.6] text-surface-base/85 max-w-[560px] font-medium mt-6 mx-auto">
      {t['calltoaction.subtitle']}
    </p>
    <a
      href={routes.contact}
      class="inline-flex items-center gap-2 mt-8 px-7 py-3.5 font-sans text-[0.9375rem] font-bold bg-surface-base text-brand-primary border-[1.5px] border-surface-base rounded-md no-underline transition-colors duration-200 hover:bg-surface-base/85"
    >
      {t['hero.cta']}
    </a>
  </div>
</section>
